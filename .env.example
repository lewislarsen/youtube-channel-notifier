# YouTube Channel Notifier Configuration
# ===================================
# === REQUIRED SETTINGS (MUST CONFIGURE) ===

# Email Notification Recipients
# For multiple recipients, use comma-separated emails
# Example: your-email@example.com,another-email@example.com
ALERT_EMAILS=

# === OPTIONAL SETTINGS ===

# Discord Webhook URL
# For notifications in a Discord channel
DISCORD_WEBHOOK_URL=

# POST Webhook URL
# For sending notifications to a custom endpoint
POST_WEBHOOK_URL=

# === EMAIL SERVER SETTINGS ===
# Mail configuration docs: https://laravel.com/docs/12.x/mail

# Mail driver (smtp, mailgun, ses, etc.)
MAIL_MAILER=smtp

# SMTP settings (required if MAIL_MAILER=smtp)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls

# The "From" address for notification emails
MAIL_FROM_ADDRESS="notifications@example.com"

# === SYSTEM SETTINGS (GENERALLY DON'T MODIFY) ===

# Database connection (sqlite is simplest for self-hosting)
# Database docs: https://laravel.com/docs/12.x/database
DB_CONNECTION=sqlite

# Application URL (localhost is fine for self-hosted installations)
APP_URL=http://localhost

# APP key (will be auto-generated during install)
APP_KEY=

# Logging level (options: emergency, alert, critical, error, warning, notice, info, debug)
# Logging docs: https://laravel.com/docs/12.x/logging
LOG_LEVEL=info
